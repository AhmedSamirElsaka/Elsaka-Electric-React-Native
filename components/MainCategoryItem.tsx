import { StyleSheet, Text, View } from "react-native";
import React from "react";

const MainCategoryItem = ({ category }: { category: string }) => {
  return <View></View>;
};

export default MainCategoryItem;

const styles = StyleSheet.create({});
