import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ProductCardCount = () => {
  return (
    <View>
      <Text>ProductCardCount</Text>
    </View>
  );
};

export default ProductCardCount;

const styles = StyleSheet.create({});
