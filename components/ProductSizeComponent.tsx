import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ProductSizeComponent = () => {
  return (
    <View>
      <Text>ProductSizeComponent</Text>
    </View>
  );
};

export default ProductSizeComponent;

const styles = StyleSheet.create({});
