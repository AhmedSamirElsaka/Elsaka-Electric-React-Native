import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ShopProductList = () => {
  return (
    <View>
      <Text>ShopProductList</Text>
    </View>
  );
};

export default ShopProductList;

const styles = StyleSheet.create({});
