import { StyleSheet, Text, View } from "react-native";
import React from "react";

const PersonalPromoCodeCard = () => {
  return (
    <View>
      <Text>PersonalPromoCodeCard</Text>
    </View>
  );
};

export default PersonalPromoCodeCard;

const styles = StyleSheet.create({});
